version: 1
on: each-value
perform:
  # transform: pass
  with:
    path: groups
    cursor:
      on: this-value
      perform:
        # transform: pass
        with:
          path: firefighters
          cursor:
            on: this-value
            perform:
              transform: list-to-object
